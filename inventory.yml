---
# reference: https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html

all:
  children:
    test:
      hosts:
        docker-test:
          ansible_host: 192.168.15.20
    #    testvm02:
    #      ansible_host: 192.168.15.92

    docker:
      hosts:
        docker.localdomain.lan:
          ansible_host: docker.localdomain.lan
