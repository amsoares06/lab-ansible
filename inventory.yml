---
# reference: https://docs.ansible.com/ansible/latest/user_guide/intro_inventory.html

all:
  hosts:
    reverse-proxy.localdomain.lan:
  children:
    dockerhosts:
      hosts:
        docker.localdomain.lan:
    kubernetes:
      hosts:
        kube00.localdomain.lan:
        kube01.localdomain.lan:
        kube02.localdomain.lan:
